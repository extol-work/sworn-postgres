# Copy to .env for local overrides (not required; defaults match docker-compose).

DATABASE_URL=postgres://sworn:sworn@localhost:5432/sworn
BIND=0.0.0.0:8080
RUST_LOG=info,sqlx=warn

# CLI: point at a running sworn-api if not localhost:8080
# SWORN_API_URL=http://localhost:8080
